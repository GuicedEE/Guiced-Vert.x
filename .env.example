# Vert.x options
VERTX_EVENT_LOOP_POOL_SIZE=8
VERTX_WORKER_POOL_SIZE=20
VERTX_CLUSTER_PUBLIC_HOST=localhost
VERTX_CLUSTER_PUBLIC_PORT=5701

# Logging
LOG_LEVEL=INFO

# GuicedEE/Guice module environment
GUICEDEE_ENV=dev

# Note: replace with deployment-specific values; do not commit secrets or credentials.
